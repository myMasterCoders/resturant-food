// src/strings/quote/quote.ts
var quote = (str, char = '"') => {
  if (str === "")
    return `${char}${char}`;
  const [first, last] = [str[0], str.at(-1)];
  if (first !== char)
    str = char + str;
  if (last !== char)
    str += char;
  return str;
};

export {
  quote
};
