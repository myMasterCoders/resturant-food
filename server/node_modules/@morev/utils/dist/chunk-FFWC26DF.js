// src/objects/deep-clone/deep-clone.ts
import { createCopier, copyStrict } from "fast-copy";
var copy = createCopier({});
var deepClone = (value) => copy(value);
var deepCloneStrict = (value) => copyStrict(value);

export {
  deepClone,
  deepCloneStrict
};
